#include<bits/stdc++.h>
using namespace std;
string check(int arr[],int n)
{
    sort(arr,arr+n,greater<int>());
    int i=0,j=1,k

    return "NO";
}

int main()
{
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
            cin>>arr[i];

    cout<<check(arr,n);
    return 0;
}
